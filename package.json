{
  "name": "project-management-board",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npx serve -l 3000",
    "test": "npx playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  }
}
